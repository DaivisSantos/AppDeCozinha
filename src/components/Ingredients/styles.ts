import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 12,
    paddingBottom: 400,
  },
  ingredientsContent: {
    gap: 12,
    paddingHorizontal: 32,
  },
})
