import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    marginBottom: 12,
    marginTop: 12,
    gap: 12,
  },
  ingredientsContent: {
    gap: 12,
    paddingHorizontal: 32,
  },
})
